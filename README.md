# generalstoreproject
HTML project to manage income and expenses for a general store

git config --global userr.name
git config --user.email
git config --list
git clone link
cd folder
notepad file
git add file
git commit -m"message"
git push origin main 
git branch -M main ""
